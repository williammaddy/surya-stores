services:
  mongodb:
    image: mongo:latest
    container_name: surya-mongodb
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: surya_stores
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
